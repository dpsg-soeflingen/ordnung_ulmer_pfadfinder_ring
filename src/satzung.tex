\documentclass[11 pt]{article}

% Configure padding around document.
\usepackage[left=2cm,right=2cm,top=2cm,bottom=2cm]{geometry}

% Configure list items for "legal" text formatting.
\usepackage{enumitem}
\newlist{legal}{enumerate}{10}
\setlist[legal]  {label*=\arabic*.}
\setlist[legal,1]{label*=\arabic*., font=\bfseries,   before=\bfseries}
\setlist[legal,2]{label*=\arabic*., font=\normalfont, before=\normalfont}

% Use `Helvetica` font instead of LaTeX default (`Computer Modern`).
\usepackage{helvet}
\renewcommand{\rmdefault}{phv} % Use Helvetica for the serif font.
\renewcommand{\sfdefault}{phv} % Use Helvetica for the sans-serif font.

% Do not indent text after newline.
\setlength\parindent{0pt}

% Define command for displaying signature of a single person.
\newcommand{\stavosignature}[0]{
    \vspace{30pt}
    \hrule
    \vspace{3pt}
    \begin{small}(Ort, Datum, Name, Unterschrift)\end{small}
}
% Define command for displaying signatures of a scouting organization's presidents.
\newcommand{\stammsignature}[1]{
    \ \\ \\ \\
    \begin{large}\textbf{#1}\end{large}
    \stavosignature
    \stavosignature
}

% Begin document & include sub-sections.
\begin{document}
    \input{sections/0_titelseite.tex}
    \newpage \pagenumbering{gobble}
    \input{sections/1_leitbild.tex}
    \newpage \pagenumbering{gobble}
    \input{sections/2_satzung.tex}
    \newpage \pagenumbering{gobble}
    \input{sections/3_unterschriften.tex}
\end{document}
